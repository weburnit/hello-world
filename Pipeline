pipeline {
  agent { dockerfile true }
  stages {
    stage('Release Image') {
      steps {
        script {
            def newApp = docker.build "weburnit/hello-world:${env.BUILD_TAG}"
            newApp.push()
        }
      }
    }
  }
}