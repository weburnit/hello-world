pipeline {
  agent { dockerfile true }
  stages {
    stage('Release Image') {
      steps {
        def newApp = docker.build "weburnit/hello-world:${env.BUILD_TAG}"
        newApp.push()
      }
    }
  }
}