pipeline {
  agent { dockerfile true }
  stages {
    stage('Release Image') {
      steps {
        newApp = docker.build "weburnit/hello-world:${env.BUILD_TAG}"
        newApp.push()
      }
    }
  }
}