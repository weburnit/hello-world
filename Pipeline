pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          docker build . -t weburnit/hello-world:jenkins
          git --version
          curl --version
          docker push weburnit/hello-world:jenkins
        '''
      }
    }
  }
}