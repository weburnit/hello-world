pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          source ~/.zshrc
          git --version
          curl --version
        '''
      }
    }
  }
}